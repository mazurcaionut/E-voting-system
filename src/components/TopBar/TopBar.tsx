import React, { useState } from "react";
import { TitleTop, TopBarRoot } from "./TopBar.styles";

export const TopBar = () => {
  return (
    <TopBarRoot>
      <TitleTop>E voting platform</TitleTop>
    </TopBarRoot>
  );
};
